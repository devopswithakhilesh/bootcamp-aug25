name: Need approval

on:
  workflow_dispatch: 

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build
        run: echo "Building..."

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment: production        # ← triggers the approval gate
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy
        run: echo "Deploying..."
